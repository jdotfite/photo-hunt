# Photo Hunt - Quick Reference Card

## 🚀 QUICK START (3 Steps)

### 1. Install Python packages
```powershell
cd c:\_websites\photo-hunt\scripts
pip install -r requirements.txt
```

### 2. Create your images
```
images/set2/image1.png  ← Original image
images/set2/image2.png  ← Modified image with 3-5 differences
```

### 3. Process and validate
```powershell
# Process single set
python find_differences.py --set 2 --tags outdoor --difficulty easy

# OR process all new sets
python batch_process.py

# Validate everything
python validate_sets.py
```

---

## 📋 COMMAND CHEAT SHEET

### Process One Set
```powershell
# Basic
python find_differences.py --set 5

# With preview
python find_differences.py --set 5 --visualize

# Full options
python find_differences.py --set 5 --tags beach,summer --difficulty medium --visualize
```

### Process All Sets
```powershell
# New sets only
python batch_process.py

# Reprocess everything
python batch_process.py --force
```

### Validate
```powershell
python validate_sets.py
```

---

## 🎯 COMMON TASKS

### Add a new set
```powershell
# 1. Create images/set6/ with image1.png & image2.png
# 2. Run:
python find_differences.py --set 6 --tags nature --difficulty easy --visualize
# 3. Test in game!
```

### Add multiple sets at once
```powershell
# 1. Create images/set7/, set8/, set9/ with images
# 2. Run:
python batch_process.py
# 3. Validate:
python validate_sets.py
```

### Reprocess a set (if differences were wrong)
```powershell
python find_differences.py --set 3 --force
```

### Check for problems
```powershell
python validate_sets.py
```

---

## 🔧 PARAMETERS GUIDE

### Difficulty Levels
- `--difficulty easy` → 2-3 differences
- `--difficulty medium` → 4-5 differences  
- `--difficulty hard` → 6+ differences

### Detection Tuning
- `--min-area 600` → Detect smaller differences
- `--min-area 1500` → Only detect larger differences
- `--merge-distance 30` → Keep differences separate
- `--merge-distance 80` → Merge nearby differences

### Tags (comma-separated)
```
outdoor, indoor, nature, urban, animals, people, 
beach, city, food, vintage, modern, etc.
```

---

## 📁 FILE LOCATIONS

```
images/setN/image1.png    ← Your images go here
images/setN/image2.png    ← Your images go here
data/sets.json            ← Auto-generated by scripts
scripts/*.py              ← Python scripts
```

---

## ⚠️ TROUBLESHOOTING

**Problem:** "Import cv2 could not be resolved"  
**Fix:** `pip install opencv-python`

**Problem:** "No differences found"  
**Fix:** Use `--min-area 600` to detect smaller differences

**Problem:** "Too many differences found"  
**Fix:** Use `--min-area 1500` to only detect larger ones

**Problem:** Images not loading in game  
**Fix:** Check paths in sets.json match actual files

---

## 💡 PRO TIPS

✅ Always use `--visualize` first to preview detection  
✅ Aim for 3-5 clear differences per set  
✅ Run `validate_sets.py` before deploying  
✅ Use `batch_process.py` for multiple sets  
✅ Keep image dimensions consistent (e.g., 1244x1866)
